namespace Citadels.Core.Districts.Special;

internal class Library : District
{
    public Library(string name, DistrictKind kind, int buildPrice) 
        : base(name, kind, buildPrice)
    {
    }
}
